const config = {
  // if there is a new user, first difference will contain all ads, send those ads too?
  sendInitialMailToNewUser: true,

  // If we added a new URL for user, first difference will contain all ads from this URL, send those ads too?
  sendMailWithInitialContentFromNewURL: true
};

module.exports = config;
